---
"@libris/api-hono": patch
"@libris/web": patch
---

Bump `@hono/node-server` 2.0.4 → 2.1.0 and `vue-router` 5.1.0 → 5.2.0.

The `@hono/node-server` bump resolves the advisory flagged in the security audit;
`vue-router` is a patch bump on a stable line.
