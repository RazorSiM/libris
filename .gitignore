# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.*
!.env.example
!.env.test.example
tmp/
.devenv/
.direnv/
node_modules/
dist/

# The SPA bundle, copied here so Hono serves the API and the frontend from one
# port. CI and the E2E harnesses create it with `cp -r apps/web/dist public`,
# so it is build output that happens to sit at the repo root — untracked and
# one `git add -A` away from being committed by accident.
/public/

# pnpm content-addressable store (CI places it inside the workspace)
.pnpm-store/

*.db

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/

# Generated at build time — run `pnpm run build:spec` to regenerate
services/api-hono/openapi.json

# Bruno API collection — generated from OpenAPI via `vp run bruno:import`
bruno/
